# Cloudflare Pages 的跳转 / 改写规则，按从上到下第一条匹配的执行。
# 这些规则在静态文件层处理，不经过 Worker，所以打开文档页不消耗 Workers 请求额度。

# 旧的文档首页地址，现在首页在根路径
/doc     /    301
/doc/    /    301

# 所有文档页（只读链接、编辑链接、/doc/new）都交给同一个编辑页，页面自己从地址里取文档 ID。
# 用 200（改写）而不是 301：地址栏保持 /doc/xxx 不变
/doc/*   /editor    200
